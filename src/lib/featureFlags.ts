const normalizeFlag = (value?: string) => (value ?? "").toLowerCase().trim();

export const AI_FEATURE_ENABLED = false;
export const ULTRA_PLAN_ENABLED = false;
export const EXAM_READINESS_ENABLED = false;
