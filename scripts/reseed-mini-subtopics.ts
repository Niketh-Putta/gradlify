console.error(
  [
    'This TypeScript script is intentionally disabled (no TS runner configured).',
    'Use the Node.js script instead:',
    '  node scripts/reseed-mini-subtopics.mjs',
  ].join('\n')
);
process.exit(1);
